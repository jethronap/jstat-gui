package detail.compute;

/**
 * Enumeration for the computation models that
 * JStat supports
 */
public enum ComputationModelType {

    EDA,
    LINEAR_REGRESSION,
    LOGISTIC_REGRESSION,
    CLUSTERING,
    INVALID_MODEL // keep this always last
}
